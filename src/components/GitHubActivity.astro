---
// GitHub Activity Component - Shows contribution graph and stats
---

<div
  class="bg-lightercontentbackground border border-primary-600 rounded-xl p-6"
>
  <div class="flex items-center justify-between mb-6">
    <h3 class="text-xl font-bold">GitHub Activity</h3>
    <a
      href="https://github.com/doguyilmaz"
      target="_blank"
      rel="noopener noreferrer"
      class="text-primary-400 hover:text-primary-300 transition-colors"
    >
      View Profile →
    </a>
  </div>

  <!-- GitHub Stats -->
  <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
    <div class="text-center">
      <div class="text-2xl font-bold text-green-400 mb-1">500+</div>
      <div class="text-sm text-primary-300">Contributions</div>
    </div>
    <div class="text-center">
      <div class="text-2xl font-bold text-blue-400 mb-1">25+</div>
      <div class="text-sm text-primary-300">Repositories</div>
    </div>
    <div class="text-center">
      <div class="text-2xl font-bold text-purple-400 mb-1">15+</div>
      <div class="text-sm text-primary-300">Languages</div>
    </div>
    <div class="text-center">
      <div class="text-2xl font-bold text-orange-400 mb-1">3+</div>
      <div class="text-sm text-primary-300">Years Active</div>
    </div>
  </div>

  <!-- Contribution Graph Placeholder -->
  <div class="space-y-3">
    <h4 class="text-sm font-medium text-primary-200">Recent Activity</h4>
    <div class="grid grid-cols-12 gap-1">
      {
        Array.from({ length: 84 }, (_, i) => (
          <div
            class={`w-3 h-3 rounded-sm ${
              Math.random() > 0.7
                ? "bg-green-500"
                : Math.random() > 0.5
                ? "bg-green-400"
                : Math.random() > 0.3
                ? "bg-green-300"
                : "bg-primary-700"
            }`}
            title={`${Math.floor(Math.random() * 10)} contributions`}
          />
        ))
      }
    </div>
    <div class="flex justify-between text-xs text-primary-400">
      <span>Less</span>
      <span>More</span>
    </div>
  </div>

  <!-- Recent Repositories -->
  <div class="mt-6 space-y-3">
    <h4 class="text-sm font-medium text-primary-200">Recent Projects</h4>
    <div class="space-y-2">
      <div
        class="flex items-center justify-between p-3 bg-primary-800/30 rounded-lg"
      >
        <div class="flex items-center space-x-3">
          <div class="w-3 h-3 bg-yellow-400 rounded-full"></div>
          <span class="text-sm font-medium">doguyilmaz.com</span>
        </div>
        <span class="text-xs text-primary-400">TypeScript</span>
      </div>
      <div
        class="flex items-center justify-between p-3 bg-primary-800/30 rounded-lg"
      >
        <div class="flex items-center space-x-3">
          <div class="w-3 h-3 bg-blue-400 rounded-full"></div>
          <span class="text-sm font-medium">tourism-analytics</span>
        </div>
        <span class="text-xs text-primary-400">React</span>
      </div>
      <div
        class="flex items-center justify-between p-3 bg-primary-800/30 rounded-lg"
      >
        <div class="flex items-center space-x-3">
          <div class="w-3 h-3 bg-green-400 rounded-full"></div>
          <span class="text-sm font-medium">mobile-tourism-app</span>
        </div>
        <span class="text-xs text-primary-400">React Native</span>
      </div>
    </div>
  </div>
</div>
